{{- if and .Values.crowdsieve.enabled .Values.crowdsieve.analyzers.enabled .Values.crowdsieve.analyzers.rules }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "crowdsieve.fullname" . }}-analyzers
  labels:
    {{- include "crowdsieve.labels" . | nindent 4 }}
data:
  {{- range $filename, $content := .Values.crowdsieve.analyzers.rules }}
  {{ $filename }}: |
    {{- $content | nindent 4 }}
  {{- end }}
{{- end }}
